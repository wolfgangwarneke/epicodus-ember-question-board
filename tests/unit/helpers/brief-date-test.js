import { briefDate } from 'first-question-board/helpers/brief-date';
import { module, test } from 'qunit';

module('Unit | Helper | brief date');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = briefDate([42]);
  assert.ok(result);
});
